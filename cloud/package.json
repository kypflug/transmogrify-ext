{
  "name": "transmogrifier-cloud",
  "version": "1.0.0",
  "type": "module",
  "description": "Azure Functions backend for async transmogrification",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "jest"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/storage-queue": "^12.23.0",
    "@mozilla/readability": "^0.5.0",
    "linkedom": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "azure-functions-core-tools": "^4.0.0",
    "rimraf": "^6.0.0",
    "typescript": "~5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
