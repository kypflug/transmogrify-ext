# Transmogrifier Extension
#
# All AI keys, image keys, and cloud config are managed via the
# Settings UI (⚙️) and stored in encrypted local/cloud storage.
# No .env vars are required to build or run the extension.
#
# Settings are encrypted at rest (AES-256-GCM) and can be synced
# across devices via OneDrive.
